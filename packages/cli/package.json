{
  "name": "@correspond-os/cli",
  "version": "0.1.0",
  "description": "CLI interface for CorrespondOS",
  "type": "module",
  "bin": {
    "correspond": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "test:ci": "bun test --coverage",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "@correspond-os/core": "workspace:*",
    "@correspond-os/shared": "workspace:*",
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
